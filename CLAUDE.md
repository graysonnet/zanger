## Agent skills

### Issue tracker

Issues are tracked locally as markdown files under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default label strings (`needs-triage`, `ready-for-agent`, etc.) are used. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (one `CONTEXT.md` at the repo root). See `docs/agents/domain.md`.